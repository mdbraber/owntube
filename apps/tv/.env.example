# Copy to .env and point at your OwnTube web instance. Nothing here belongs in
# the repo.
#
# On the Android emulator the host machine is reachable at 10.0.2.2, so a web
# app running locally on port 3000 is http://10.0.2.2:3000.
EXPO_PUBLIC_OWNTUBE_URL=http://10.0.2.2:3000
