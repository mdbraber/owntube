/** Solid brand accent — matches `--primary` / favicon (#f23662). */
export const BRAND_COLOR = "#f23662";

/** @deprecated Use BRAND_COLOR — kept for channel avatar palette compatibility. */
export const BRAND_GRADIENT = BRAND_COLOR;
